# 待修改项 - ML307R MQTT SSL加密 & 初始化流程

## 已完成 ✅

### 1. `\r\n` 命令格式
- **文件**: `app/src/uart_at.c:164`
- **状态**: ✅ 已正确实现
- **代码**: `snprintf(cmd_buf, sizeof(cmd_buf), "%s\r\n", cmd);`

### 2. AT+CSQ 信号质量检查
- **文件**: `app/src/ml307r.c`
- **状态**: ✅ 已添加
- **说明**: 在 AT+CPIN? 之后、AT+CEREG? 之前添加

### 3. AT+CGATT? 网络附着状态检查
- **文件**: `app/src/ml307r.c`
- **状态**: ✅ 已添加
- **说明**: 在 AT+CSQ 之后、AT+CEREG? 之前添加

### 4. MQTT SSL加密
- **文件**: `app/src/at_parser.c:87-93`
- **状态**: ✅ 已修复
- **修改**: 在 `at_mqtt_config()` 中添加 `AT+MQTTSSL=1` 命令

---

## 修改详情

### 修改1: `app/src/ml307r.c` - 初始化流程 (已完成 ✅)

**位置**: `ml307r_init()` 函数

**新增内容** (在 AT+CPIN? 和 AT+CEREG? 之间):

```c
// 4. 查询信号质量 AT+CSQ（建议信号 > 18）
DEBUG_4G_PRINTF(" >>> AT send: AT+CSQ\r\n");
ret = at_send_command("AT+CSQ", "OK", AT_TIMEOUT_DEFAULT, resp, sizeof(resp));
DEBUG_4G_PRINTF(" <<< AT+CSQ resp: %s, ret=%d\r\n", resp, ret);
if (ret == 0)
{
  int rssi = 99;
  (void)sscanf(resp, "+CSQ: %d", &rssi);
  if (rssi < 10 || rssi > 31)
  {
    DEBUG_4G_PRINTF(" !!! Signal quality poor (RSSI=%d) !!!\r\n", rssi);
  }
  else
  {
    DEBUG_4G_PRINTF(" OK - Signal quality OK (RSSI=%d)\r\n", rssi);
  }
}

// 5. 查询网络附着状态 AT+CGATT?
DEBUG_4G_PRINTF(" >>> AT send: AT+CGATT?\r\n");
ret = at_send_command("AT+CGATT?", "OK", AT_TIMEOUT_DEFAULT, resp, sizeof(resp));
DEBUG_4G_PRINTF(" <<< AT+CGATT? resp: %s, ret=%d\r\n", resp, ret);
if (ret == 0 && strstr(resp, "+CGATT: 1") != NULL)
{
  DEBUG_4G_PRINTF(" OK - Network attached\r\n");
}
```

**步骤编号变更**:
- 原步骤4 (CEREG) → 现为步骤7
- 原步骤5 (MIPCALL) → 现为步骤8

---

### 修改2: `app/src/at_parser.c` - MQTT SSL加密 (已完成 ✅)

**位置**: `at_mqtt_config()` 函数 (第82-99行)

**修改前**:
```c
int at_mqtt_config(const char *host, int port, const char *client_id,
                   const char *username, const char *password) {
  char cmd[512], resp[256];
  snprintf(cmd, sizeof(cmd), "AT+MQTTCONN=%d,\"%s\",%d,\"%s\",\"%s\",\"%s\"",
           MQTT_CONN_ID, host, port, client_id, username, password);
  return at_send_command(cmd, "OK", AT_TIMEOUT_LONG, resp, sizeof(resp));
}
```

**修改后**:
```c
int at_mqtt_config(const char *host, int port, const char *client_id,
                   const char *username, const char *password) {
  char cmd[512], resp[256];
  int ret;

  // 启用MQTT SSL加密 (MQTT_PORT=8883为SSL端口，必须先启用SSL)
  ret = at_send_command("AT+MQTTSSL=1", "OK", AT_TIMEOUT_DEFAULT, resp, sizeof(resp));
  if (ret != 0) {
    DEBUG_4G_PRINTF("[MQTT] !!! MQTT SSL enable failed !!!\r\n");
    return -1;
  }
  DEBUG_4G_PRINTF("[MQTT] OK - MQTT SSL enabled\r\n");

  // 配置MQTT连接参数
  snprintf(cmd, sizeof(cmd), "AT+MQTTCONN=%d,\"%s\",%d,\"%s\",\"%s\",\"%s\"",
           MQTT_CONN_ID, host, port, client_id, username, password);
  return at_send_command(cmd, "OK", AT_TIMEOUT_LONG, resp, sizeof(resp));
}
```

---

## 完整初始化流程 (修复后)

| 步骤 | AT命令 | 说明 |
|------|--------|------|
| 1 | AT | 握手测试 |
| 2 | ATE0 | 关闭回显 |
| 3 | AT+CPIN? | 检查SIM卡状态 |
| 4 | AT+CSQ | **新增** - 查询信号质量 |
| 5 | AT+CGATT? | **新增** - 查询网络附着状态 |
| 6 | AT+CEREG? | 查询网络注册状态 (最多30次重试) |
| 7 | AT+MIPCALL=1,1 | 激活PDP拨号 |
| 8 | AT+MQTTSSL=1 | **新增** - 启用MQTT SSL |
| 9 | AT+MQTTCONN=... | 连接MQTT服务器 |

---

## 验证清单

- [ ] 编译通过无错误
- [ ] 烧录后串口日志显示 `AT+CSQ` 查询
- [ ] 串口日志显示 `AT+CGATT?` 查询
- [ ] 串口日志显示 `[MQTT] OK - MQTT SSL enabled`
- [ ] MQTT连接成功 (`+MQTTURC: "conn",0,0`)
- [ ] 设备成功上报数据到云端
